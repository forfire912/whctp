# CTP期货交易管理系统 - Python依赖包

# ========== 必需依赖 ==========

# 数据库驱动（MySQL）
pymysql>=1.0.2

# ========== CTP API（核心功能）==========

# OpenCTP - 开源CTP API Python绑定（推荐）
# 支持Windows x64，需要Python 3.7+
# 如果安装失败，程序会自动切换到模拟模式
openctp-ctp>=6.6.9

# 备注：如果从期货公司获取了官方CTP SDK，可以使用对应的Python绑定包

# ========== 可选依赖 ==========

# 数据处理（用于数据分析和导出）
pandas>=1.3.0

# PDF文档读取（用于读取API文档）
PyPDF2>=3.0.0

# 日志增强（可选，用于更好的日志管理）
# loguru>=0.6.0

# YAML配置文件支持（可选）
# pyyaml>=6.0

# 日期时间处理增强（可选）
# python-dateutil>=2.8.0

# ========== 系统自带模块（无需安装）==========
# tkinter - GUI库（Python内置）
# json - 配置文件处理（Python内置）
# threading - 多线程（Python内置）
# csv - CSV处理（Python内置）
# datetime - 时间处理（Python内置）
# logging - 日志（Python内置）

# ========== 安装说明 ==========
# 
# 基本安装（仅数据库和界面）：
#   pip install pymysql
#
# 完整安装（包含CTP API）：
#   pip install -r requirements.txt
#
# 使用国内镜像加速：
#   pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
#
# 如果openctp-ctp安装失败：
#   程序会自动使用模拟模式，可以正常使用界面和数据库功能
#   
# Windows用户注意：
#   需要安装Visual C++ Redistributable
#   下载地址：https://aka.ms/vs/17/release/vc_redist.x64.exe
